import React from "react";
import { Box, Text } from "rebass";

export type props = {};

export const Footer = (({}: props) => {
  return (
    <Box bg="pink">
      <Text>Footer</Text>
    </Box>
  );
});
