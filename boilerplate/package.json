{
  "name": "hookedjs-project-example",
  "author": "Brian Dombrowski <bdombro@gmail.com>",
  "version": "0.0.1",
  "license": "ISC",
  "scripts": {
    "upgrade-hard": "npx ncu -u && yarn && yarn upgrade",
    "prettier": "prettier --loglevel warn --write \"{client-web/src,server-koa2/src}/**/*.{ts,tsx,css,json,md,js}\"",
    "types": "tsc --noEmit",
    "types:watch": "tsc --noEmit --watch",
    "start": "hookedjs -s",
    "build": "hookedjs -b",
    "dev": "hookedjs -d",
    "test": "hookedjs -t",
    "clean": "rm -rf build node_modules"
  },
  "private": false,
  "husky": {
    "hooks": {
      "pre-commit": ""
    }
  },
  "dependencies": {
    "hamburgers": "^1.1.3",
    "hookedjs": "file:../../hookedjs",
    "husky": "^1.3.1",
    "prettier": "^1.16.4",
    "react-responsive-modal": "^3.6.0"
  },
  "engines": {
    "node": ">11.7",
    "npm": ">=9999.0.0",
    "yarn": ">=1.9.4"
  }
}
